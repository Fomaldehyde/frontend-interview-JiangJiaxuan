
// 编写run函数，使该代码能正常运行，不报错

function run() {
    // 执行一段代码
    const [a, b] = [1, 2];
    console.log(a, b); // 输出 1 2
  }
  run();
  //const [a,b] = {a:1,b:2};// 报错，因为解构赋值的右侧应该是数组或者对象
  //console.log(a, b); // 输出 1 2